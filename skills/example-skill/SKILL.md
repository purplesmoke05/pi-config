---
name: example-skill
description: Placeholder skill that documents the expected SKILL.md layout. Replace with a real skill; this text decides when the agent loads it, so be specific.
---

# Example Skill

This is a placeholder. A skill directory contains a `SKILL.md` (required) plus
any helper scripts or reference docs it needs:

```
my-skill/
├── SKILL.md
├── scripts/
└── references/
```

Reference bundled files with relative paths from the skill directory.
